<% content_for :header do %>
  <% if @logged_in %>
    <%= button_to "Logout", action: 'logout',  params: { id: @id } %>
  <% else %>
    <%= link_to "Login", "/users/login", class: 'header-link'  %>
  <% end %>
<% end %>

<h1 class='mb-2 font-bold text-xl'>Users</h1>
<%= link_to "Login", "/users/login" %>

<ul>
  <% @users.each do |u| %>
    <li class='mb-2'>
      Username: <%= link_to u.username, "/users/#{u.id}" %> <br/>
      Password: <%= u.password.split('').map{'•'}.join %><br/>
      Active: <%= u.active %><br/>
    </li>
  <% end %>
</ul>

<%= link_to "New User", '/users/new' %>
