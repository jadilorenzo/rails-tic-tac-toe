<% content_for :header do %>
  <% if @logged_in %>
    <%= button_to "Logout", controller: :users, action: 'logout',  params: { id: @id } %>
  <% else %>
    <%= link_to "Login", "/users/login", class: 'header-link'  %>
  <% end %>
<% end %>
